contributions widgets tooltips menu toolbar items include key bindings tooltips include short cut key tooltip resume button resume short cut key displayed newline resume feature configurable turned experienced users configured resume resume resume
