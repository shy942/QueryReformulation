compatibility jubula functional text execution perspective blank perspective blank reasons layout code reproduce problem public void create initial layout layout layout string editor area layout editor area folder layout left layout create folder left layout editor area folder layout top left layout create folder top left layout left folder layout top layout create folder top layout editor area folder layout bottom layout create folder bottom layout top folder layout middle layout create folder middle layout editor area folder layout middle bottom layout create folder middle bottom layout middle left add view layout top left add view layout middle add view layout middle bottom add view layout top add view layout bottom add view layout
