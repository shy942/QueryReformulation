import export import projects wizard finish enabled project selected import existing projects wizard finish button enabled projects selected import import wizard invoked finish disabled projects selected import directory projects selected finish remains disabled directory project selected finish enabled projects selected import finish remains enabled behaviour cause functional problems inconsistent
 