99840.txt: preferences preference repainted dialog container size equals container size preference dialog creates preference objects preference dialog displayed preference selected default displayed correctly select preference preference dialog title body resize displayed noticed select preference default preference displayed correctly note regression eclipse behavior preference dialog stepped prefence observed code problem lies point content size size resizing computation needed inserted computing dialog size calling dialog open prevent auto resize user manually resized shell shell shell point shell size shell size null rectangle rect container client area point container size point rect width rect height int hdiff content size container size int vdiff content size container size hdiff vdiff shell size equals shell size hdiff math max hdiff vdiff math max vdiff set shell size shell size hdiff shell size vdiff shell size shell size current control size current control set size container size current set size container size hdiff vdiff current set size container size reason problem area control set size preference set size executed preference repainted case preference dialog container size client area preference identical set size called current control set size container size called preference selected container size
