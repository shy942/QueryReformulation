92868.txt: forms ambiguous spec managed form eclipse forms classes expect managed form interface implementing required managed form subclassed managed form public interface managed form unclear spec clients allowed implement managed form choose intent managed form spec clear standard boilerplate initialize cycle breaking change case
